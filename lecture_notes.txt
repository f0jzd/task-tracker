Node and NPM keywords

npm init -y //Lägga till NPM till projektet
npm install typescript --save-dev //installera typescript som developer dependency
npx tsc --init // execute typescript initialize / lägg till typescript till projektet, lägger till package.json
npx tsc // kompliera typescript filen
npm run watch //kör watch scriptet i package.json filen

tsc betyder typescript compile
npx = node package execute
npm = node package manager

---------------------------------------------------

Variabler

let = variabl vi kan ändra värdet på
const kan man inte ändra värdet på.
var = försök undiv var och använd var och const istället

datatyper

naming convetion : inte 123 t.ex. inte börja på en siffra, variabl namn måste betså av bokstäver eller börja på ett underscore eller ett dollar tecken. 
standard är camelcase. 

stepover = step by step

any och unknown
any "stänger av typescxript"

modulo exempel : odd and even, begräna iterreringen av en array, vanligt vid tester

------------------------------------------------------------------

Array och If-satser

.push = sätter till ett element i slutet av arrayen
.pop = tar bort elementet på slutet av arrayen, tar bort och är inte en referens till slutet
.shift och .unshift gör samma fast i börjanm av arrayen, unshift lägger till, shift tar bort. 

for loop för index, när behöver hålla koll på varven
for of när man ska gå igenom en array, för varje task i array t.ex. visa alla uppgifter
foreach när man vill göra något med varje element, ska visa alla, ska köra samma kodsnutt på varje
while är bra när man inte vet hur många gånger loopen ska köra. 
do while samma som while, men göra en sak minst en gång. 

--------------------------------------------------------------------

Type/ Interface

type = inte data, bara beskrivning av data
ts använder beskrivningen för att hitta fel.

type Status = "Pending" | "Completed" => kan bara vara dem två strängarna

union types = ett av flera bestämda val

Interface = kontrakt, beskriver exakt vilka properties ska finnas i objektet, något man måste följa
type = allt möjligt hit och dit
Interface = objekt, kontrakt för objekt, hur de ska vara, mer styrt och bestämt

type vs interface välj det som passar situationen, förstå båda och följ arbetsplatsen.

funktion i objekt = metod

typning gör saker mer klart och tydligt. typ som kommentarer i kod

unions försvinner när typescript komplieras, enums do not. 
union enklare, enums värden med namn

------------------------------------------------------------

Query selector = hitta första som matchar parametern.


----------------------------------

Make array into interface , mehtod ot add/ append task to the list create task thing

////////////////////////////////////////
kalal argument inkajshd

.checkValidity()

ctrl k ctrl 0

wrappa append för enklare styling

selectedIndex

modulärt, varje fil är en modul, en egen funktion som är lokalt scopat

flera projekt
/////////////////////////////////

klasser för screnreadeers endast är en grej, kan skippa labels

////////////////////////////////

Spread operator, packa upp det som är i parametern

spread operator kan anvöändas som copy istället för referens

////////////////////////////////////////////////

stringify - > json  

parse -> variabel

localstorage.setitem för att spara
localstorage.getitem för att hämta
json.stringify för att array/objekt till text
json.parse omvandla sträng tillbaks till objekt

spara som string -> stringify 
läsa som string -> parse

varjae ändring måste kalla och uppdatera tasks

local storage = saker som t.ex. språk, dark mode, shoppping kart, saker som ska sparas. inställningar i guess. 
SÄTT INTE PASSORD SAKER I LOCAL STORAGE, INTE FÖR VIKTIG INFORMATION
lagra inte mycket data heller, 50-100 inställningar okej, 1000+ bilder big nono, preferenes och val borde sparas.
bra för små data

session storage = spara under sessionen, startar man om weblässaren försvinner den, kan användas som små inställningar när man surfar som inte behöver sparas.


extension storage = om man har extensions i chrome så sparar den sin data i extension storage. 


cookies tl:dr tllhör den tredje sparningtekniken, cookies används för inloggningar, session id, språkinställning, analys.
cookies skickas med varje https call

/////////////////////////////////////////////////////

